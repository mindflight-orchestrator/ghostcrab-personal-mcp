# demo/hello-world

A vendored skill shipped with **examples/remote-demos** for offline demos.

It is installed into the GhostCrab local skill store via:

`gcp agent skills install --dir ./vendor/demo/hello-world`

(or `gcp skills install --dir …`)

No registry or token required.

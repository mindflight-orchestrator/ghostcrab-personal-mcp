-- Reserved for future self-describing bootstrap data.
-- The phase 1 server intentionally exposes no business tools yet,
-- so the canonical mfo:system seed will land with the first usable tool surface.
SELECT 1;

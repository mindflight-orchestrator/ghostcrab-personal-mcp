# OpenClaw Agent: GhostCrab Epistemic Agent

This is the fuller OpenClaw profile built on top of the `ghostcrab-memory` skill.

Use it when you want:

- a persistent persona
- explicit gap handling
- repeated startup and heartbeat behavior
- a stronger demo of GhostCrab as an epistemic stack
- fuzzy request routing into the correct activity family before modeling
- explicit checkpoint and transition discipline for long-running work

This profile is intentionally generic enough to work across the shipped demo profiles.

**OpenClaw wiring (MCP, skills layout, scenarios):** [../README.md](../README.md).

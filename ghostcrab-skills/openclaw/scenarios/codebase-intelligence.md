# OpenClaw Scenario: Codebase intelligence

## Guided

1. `Use ghostcrab_status, then ghostcrab_search with filters on facets such as path_top_level="shared" and domain="codebase-intelligence" to list path-qualified demo facts.`
2. `Use ghostcrab_search with syntax="jsonl" or path_extension="jsonl" to isolate data-shaped portable profile files.`
3. `Use ghostcrab_traverse from start="concept:codebase-intelligence:path-qualified-ingest" with outbound EXTRACTED_FROM and REFERENCES edges.`
4. `Use ghostcrab_pack for scope repo-facet-rollout and summarize path/content facet dimensions without duplicating full file bodies.`

## Semi-Autonomous

`Je veux indexer mon depot comme source de verite: chaque fait durable doit rappeler le chemin du fichier, le type de contenu (doc, code, donnees), et la section logique, pour pouvoir filtrer et compter avec ghostcrab_search.`

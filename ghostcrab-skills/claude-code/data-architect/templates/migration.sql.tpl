-- ${MIGRATION_NAME}
-- Create or register project-specific schema metadata here.

-- Example placeholder:
-- INSERT INTO mfo_facets (schema_id, content, facets)
-- VALUES (
--   'mfo:schema',
--   '{"schema_id":"project:example:entity","description":"..."}',
--   '{"schema_id":"project:example:entity","target":"facets","version":1}'
-- );
